import ConversationContainer from "@/components/Shared/conversation/conversationContainer";

const Conversations = () => {
  return (
   <ConversationContainer>
       hello from conversations id
   </ConversationContainer>
  );
};

export default Conversations;
